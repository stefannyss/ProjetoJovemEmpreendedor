var btn = document.querySelector('#voltaraotopo')
btn.addEventListener('click', function () {
  window.scrollTo(0, 0)
})

/* define uma hash (ou id) para o comando btn, quando clicar no botão na pag html,
ele abre um evento "click" e faz com que a tela volte ao topo com o Scrollto (0,0) "faz um scrol até o topo"*/
